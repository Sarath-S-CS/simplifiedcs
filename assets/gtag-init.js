// Google Analytics (gtag.js) bootstrap - the standard snippet Google
// provides, moved out of an inline <script> in src/head.html so the site's
// Content-Security-Policy (_headers) can use a plain script-src 'self'
// instead of a per-byte hash that would silently break on any edit or
// line-ending change to the inline copy.
window.dataLayer = window.dataLayer || [];
function gtag(){dataLayer.push(arguments);}
gtag('js', new Date());

gtag('config', 'G-M10MSC9BG2');
